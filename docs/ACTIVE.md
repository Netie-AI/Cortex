# docs/ACTIVE.md
Current map. Law: `CLAUDE.md`. State: `STATUS.md`. Deferred: `PARKING_LOT.md`.

## Running
- Engine `:8011` -- `GET /health` (specs need viewer `X-API-Key`).
- Crew converse `:8020`. Control estate `:8040` (display-only).

## Gated this tree
- LOOP-01/02, EPIC-016, SEC-01
- Wave 1: badge-fail-closed, dep floors, lakehouse `connect_write`, chat route once
- Docs honesty (WASM row, contract 1.2.0, STATUS header, baseline >=330)

## Branch cap (from 2026-09-03)
Max 4 live heads: `main`, the seated writer, +2 lanes. No new worktree per ticket. Seat existing writers. Close a branch when its unique patch is already on the writer HEAD.

## Do not start here
- Founder-blocked: GitHub #12 #13 #17 #18 #42
- Held PRs: #4 #41 #43 #44 #70 #71
- Other branch: p22 SDK reads, adv-bench, answer-path repairs
- Other repo: Netie-KB, Pointer #29, OpenVault #38/#39, DMS OpenAPI pin
- Parked from `cursor/constructor-session-json`: `workspace.*` fetch in `constructor_fetch.py`, `load_inventory_csv` via `warehouse_path()`. Add when Constructor needs workspace slices. Do not re-land crew tests from `f598efa`.
