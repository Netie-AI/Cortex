# Netie global rule blocks — corrected (2026-07-31)

Claude’s Block B (“Cursor = UI only; migrations/CI/security → Claude Code only”) is **wrong**.
Both products do full Netie work. Difference is **model lanes and subagent shape**, not a capability ghetto.

| | Claude Code | Cursor |
|---|---|---|
| Plan / judge | Opus | Grok 4.5 |
| Execute | Fable | Composer 2.5 |
| Research lenses | Agent teams / Task | Task explore/review/… |
| Optional breadth | — | GPT 5.6 |
| Global file | `~\.claude\CLAUDE.md` | User Rules paste + `~\.cursor\rules\netie-global.mdc` |

Composer is the **implement lane** after a plan exists — analogous to Fable — not a “subagent that only does React.” Task-tool agents are the research/lens subagents.

---

## Block A — Claude Code (already written to disk)

**Destination:** `C:\Users\OoiJianHong\.claude\CLAUDE.md` (loaded as Claude Code user/global rules)

Open that file — it is the paste source. Keep ≤60 lines.

---

## Block B — Cursor User Rules (paste today)

**Destination:** Cursor → Settings → Rules → User Rules  
**Also on disk:** `C:\Users\OoiJianHong\.cursor\USER_RULES_PASTE.md`  
**Always-apply mirror:** `C:\Users\OoiJianHong\.cursor\rules\netie-global.mdc`

Paste the contents of `USER_RULES_PASTE.md` into User Rules.

---

## Block C — after KB-1 + KB-2 (swap later)

Replace the **Invariants** section in both A and B with:

```markdown
## Invariants
Read D:\Netie-KB\INDEX.md. All rules with status: active are binding.
Regenerated by `python D:\Netie-KB\scripts\kb.py render` — do not hand-edit this section.
```

Then `kb.py render` writes the full list into `.claude\CLAUDE.md` and
`.cursor\rules\netie-kb.mdc` so you stop maintaining two copies by hand.

Until KB exists, keep R1–R11 inline (as now).

---

## Block D — per-repo pointer (append)

Append to `D:\Cortex\CLAUDE.md` and `D:\DMS\CLAUDE.md`:

```markdown
## Knowledge base
Global invariants and workflows live in D:\Netie-KB, not in this repo.
Before non-trivial work: `python D:\Netie-KB\scripts\kb.py search "<keywords>"`
(or preflight `docs/subagents_findings/INDEX.md` if KB missing).
After: file a finding. Do not duplicate global R1–R11 here — this file holds
only repo-specific laws (import boundaries, contract freezes, pack rules).
```

---

## Order

1. Paste **Block A** (Claude) and **Block B** (Cursor) now — standalone.
2. Run KB-1 scaffold + KB-2 seed when ready (`D:\Netie-KB` still missing today).
3. Swap **Block C**, add **Block D**.
4. KB-3 tool sync (`kb.py render`).
5. KB-4 mine `~\.claude\tasks\<uuid>\` transcripts into findings.

Keep each global surface **~60 lines**. Diluted rules get skimmed.
