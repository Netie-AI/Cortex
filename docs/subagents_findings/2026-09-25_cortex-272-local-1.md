# Finding: Cortex #272 LOCAL-1 through OpenVault

- **Date:** 2026-09-25
- **Keywords:** freeroute, openvault, served_local, served_provider, LOCAL_ONLY, local_qwen, #272, #71, #269
- **Main idea:** Cortex stamps `served_provider` / `served_model` / `served_local` from the OpenVault response only. Arming may accept an OpenVault-reported local spendable hop (`local_qwen`). `CORTEX_FREEROUTE_LOCAL_ONLY=1` is fail-closed (drop answer, no cloud fallback). OV#71 confirms those five wire names plus 503 `openvault_local_only_unavailable` (`local_unreachable` / `local_model_not_loaded` / `local_base_url_not_loopback`) and 403 `openvault_vault_sealed`. Isolated in `CortexOS/integrations/freeroute_ov_local.py`. #269 owns store/`pick`.
- **Verify:** `python -m pytest tests/test_freeroute_local.py tests/test_freeroute_core.py tests/dms/test_insights_router_fingerprint.py tests/test_freeroute_router1.py -q`
- **Does not prove:** live local-only (`served_local=true` on every call); GPU/quality. Ceiling: local not proven.
- **Cite:** Cortex#272 (founder GO; OV#71 contract comment 5829407860). OpenVault#71. Split with #269 comment 5828892986.
