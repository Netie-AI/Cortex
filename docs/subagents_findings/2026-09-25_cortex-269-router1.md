# Cortex #269 ROUTER-1: cost store, shadow/held-out filter, Insights fingerprint

- **Date:** 2026-09-25
- **Keywords:** freeroute, router, held-out, shadow, split, token cost, insights, served_model, learn_enabled, #269
- **Main idea:** ROUTER-1 records token usage and a split tag additively, keeps shadow/held-out/benchmark rows out of `pick` learning without changing the pick rule or the LEARN default, stamps Insights `served_*` as unproven until #272 (never inferred from the requested model), and refuses a baseline unless `CORTEX_FREEROUTE_LEARN` is explicit and `arming()` is armed with spendable_hops > 0 (reason recorded either way). Masking is off; masking-off numbers must never be compared with masking-on.
- **Verify:** `python -m pytest tests/test_freeroute_router1.py tests/dms/test_insights_router_fingerprint.py tests/test_freeroute_core.py tests/dms/test_insights_api.py tests/test_crew/test_insights.py -q --tb=short`
- **Does not prove:** live OpenVault quality, a live pack baseline, served_provider/served_local from a hop (#272), masking-on numbers (#268), escalation steps (#270), GitHub CI until the five push checks are read live.
- **Cite:** Cortex #269 comments 5828834749, 5828862487, 5828876402, 5828892986, 5829066072. Start SHA `ca801a23`.
