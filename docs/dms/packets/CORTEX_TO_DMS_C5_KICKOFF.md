# Cortex → DMS — C5 kickoff (tool classes)

**Status:** parked until T7 Wave 1 green on main.  
**Slice:** tag ontology tools with real `ToolClass`; fix `contract_tool_registry` all-`apply`; refuse agent→`apply` in `tool_runner`.

Files: `packs/dms/ontology/action_types.yaml`, `CortexOS/execution/tool_runner.py`, `CortexOS/api/contract_routes.py`.
