# Cortex → DMS — C7 kickoff (schema gate)

**Status:** parked after C8-min.  
**Slice (min):** EXPLAIN dry-run + retry×2→abstain on submit/L2 path.  
**Later:** embedding schema retrieval (stage 7).

Do not weaken `manifest.py` refusals. Files: `CortexOS/dms/sql_guardrail.py`, `CortexOS/execution/submit.py`, `CortexOS/dms/answer_engine.py`.
