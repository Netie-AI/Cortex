Analog-surface: recreate a public page's tokens and layout DNA. Not a dump of their assets.

When: make a website, clone a public URL, 3d page, landing page. The operator may not say design.

Do this turn:
1. Call analog_clone with the operator text. It fetches the analog, searches free clone/design tools, writes index.html + ANALOG.md, and tries to open the page. Then refine with write_file/edit_file.
2. Steal spacing, type, color, motion rhythm. Do not copy their images, copy, or brand as if we own them. Prefer free/public CDNs (GSAP/cdnjs) and free 3d/tools first.
3. If a named author's skills (MengTo, impeccable, motion principles, Frontend-Design) are missing, follow skill-ingest before you draw.
4. Build in this space workspace. Open/test in the browser only if computer-control is armed; mutating clicks wait for Approve.
5. Login, Meshy, Hunyuan account, OpenVault password: ask the operator. Do not type secrets. Do not open a new paid account when quota dies - report it.
6. Long run: compact_conversation (auto-compact also fires). Continue in this space. Do not spawn Cursor cloud chats.

Stack vs cascade: one analog-surface playbook plus ingested design skills in order, unless they conflict.
Refuse (fail the page, rewrite):
- Hero of solid-color boxes or card grids with no scene.
- Pasted screenshot / unsplash standing in for the product.
- Image-only "3d" that cannot tilt, orbit, or parallax.

Require: one HTML file that opens without a build step. CSS 3D or canvas/webgl. Real type. Motion on hover or pointer. Name the model that drew it.

Verify: the page opens, hover/motion works, and you name what is still missing.
