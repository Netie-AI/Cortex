<cursor_commands>

--- Cursor Command: code-review ---
Review the relevant code with a code review mindset. Prioritize bugs, behavioral regressions, security issues, and missing tests. Findings must be the primary focus, ordered by severity. Do not make code changes unless the user explicitly asks for them.

ultra feature for multi-agent cloud review that's user-triggered and billed, requiring a git repo. The rest covers documentation around claude.ai code sandbox, GitHub integration, environment configuration, secrets management, teleport, Agent SDK deployment, and the claude-code-action. Subagents are defined through frontmatter in .claude/agents/*.md files specifying model, effort, and tools, invoked via the Agent tool with a subagent_type parameter, returning a final report that gets relayed rather than shown directly to the user, with continuation possible through SendMessage.
--- End Command ---
</cursor_commands>
<timestamp>Saturday, Jul 25, 2026, 12:58 AM (UTC+8)</timestamp>
<user_query>
c:\Users\OoiJianHong\.cursor\projects\d-Cortex\agent-transcripts\9dd3524c-f350-4bb4-ab91-906a5dd6b373\9dd3524c-f350-4bb4-ab91-906a5dd6b373.jsonl 

I'm understanding what they're asking for—they want me to extract the core patterns from how Claude Code works so they can build Netie, their orchestration engine. The approach is to start with planning, then work through four parallel tracks covering memory, tools, agents, and cloud infrastructure, with all findings documented as markdown and each claim tagged with evidence type and confidence level. I need to check the repo for the "master bank" document and those untracked files to understand the format they're referencing. They mention plan mode, but since this is an autonomous session, entering plan mode would block on user approval — I need to work within the constraints of non-interactive operation and proceed with the reversible actions that follow from their request. the markdown file, then launch four parallel research lanes to investigate memory management, available tools, agent architecture, and cloud infrastructure. I'll also need to locate the master bank documentation in the repo to understand the PART 6 and PART 7 formats, check whether the skill_distill captures directory exists, and review the netie-distill rules file before diving into the Claude Code internals questions. I'll divide the work by having the spawned agents fetch documentation evidence from the official docs while I contribute what I can directly observe from my own system prompt and capabilities — things like the Agent tool, subagents, background tasks, plan mode, skills, MCP deferred tools, memory management, scratchpad, hooks, permission modes, and workflows. The agents can pull in the reference docs for settings, hooks, subagents, MCP, Claude Code on the web, and the skills CLI to round out the picture.

My approach is to first check the repo context for the master bank file and any existing distill session materials, then spin up four parallel agents to gather docs-based evidence while I draft the observed sections myself, and finally synthesize everything into a single markdown capture that includes the PART 6 matrix with the Code column formatted consistently with whatever master bank structure exists. I'm deciding whether to write a single combined capture file or separate files per lane. The template suggests one file per lane, but since the answer is an integrated document, I'll create one file at `2026-07-25_claude-code_all-lanes.md` and print that path for them to use. Finalizing the output path as `skill_distill/captures/2026-07-25_claude-code_all-lanes.md`. Now I need to locate the master bank with the decision matrix and YAML action blocks — likely in `.cursor/skills/distill-session/` or within the existing `skill_distill/` directory — and if it's not there, I'll construct reasonable formats based on the requirements.

For the plan mode question: since this is an autonomous session where real-time user interaction would block progress, I should present a concise plan upfront in my response and then execute it, rather than deferring to EnterPlanMode.

Now I'm cataloging what I've directly observed in this session—the Agent tool with its various subagent types, how subagents relay findings rather than showing full reports, the Workflow tool's orchestration capabilities with concurrency limits and budget tracking, and the Skills system with its invocation patterns.

I'm also noting the deferred tools that lazy-load on demand, the MCP servers that are available (some requiring authentication), and the persistent memory system stored locally with a structured index that reloads each session. The CLAUDE.md file gets injected as a system reminder, and there's context management logic that handles conversation growth. I'm seeing the ScheduleWakeup schema details and noting background task capabilities like bash execution and notifications. The claude CLI is available in the terminal with mcp commands, and there's a 1-hour prompt-cache TTL. I'm also checking whether subagents can spawn nested agents — the Explore tool explicitly excludes Agent, which suggests nesting limitations exist, though I need to verify the exact constraints across different agent types.

Now I'm looking at the hooks system (PreToolUse, PostToolUse, UserPromptSubmit, SessionStart, SubagentStop, PreCompact, Notification, PermissionRequest) and how permission decisions flow through allow/deny/ask rules in settings.json, with different permission modes like acceptEdits, plan, and bypassPermissions.

I need to verify the Skills CLI setup — whether it's `npx @anthropic-ai/skills` or accessed through the plugin marketplace, and confirm where the auto memory configuration lives. Subagents are defined via frontmatter in `.claude/agents/*.md` files with name, description, tools, and model, and they auto-delegate based on their description. Sessions create transcripts stored as JSONL at `~/.claude/projects/<slug>/<session-id>.json`, and I could verify this by listing the actual project directory to see what files exist there.

Plan mode cycles through options with Shift+Tab and may write plan files via ExitPlanMode. MCP configuration lives in `.mcp.json` with scopes, OAuth, and multiple transport options, plus tool discovery that this harness implements via ToolSearch.

I'm planning experiments using Playwright and CLI to validate the step-log claim—like running Claude with streaming JSON output to observe the event sequence, or spawning parallel subagents to test concurrent file writes. Handling the lazy-load validation error when tools aren't pre-fetched, then running a Playwright test against the cloud interface to verify sandbox and MCP behavior. Now I need to structure this as a comprehensive write-up with tagged claims across sections covering methodology, tools, memory systems, cloud agents, and subagents.

Starting with the repo scan — I'll glob the distill session skills, read the netie rules file, search for key markers like PART 6 and PART 7, check the skill distill directory, and examine the local project memory and transcripts. Then spinning up four parallel lane agents to handle the validation experiments and decision matrix mapping. I should read PARKING_LOT.md and STATUS.md to match the format for promote items, then spawn the four background agents in parallel while I review those files. The final deliverable needs to be a comprehensive markdown file with the full answers, so I'll need to synthesize the agent findings into that format once they return. I'll write the file with the full capture and provide a concise summary in chat along with the path. Now I need to locate the master bank file to find the PART 6 decision matrix rows — I'll search through the cursor rules and skill distill directories, then grep for PART 6 and PART 7 references to pull the exact rows I need to fill in the Claude Code column. Each lane agent needs to gather specific claims with evidence links, confidence levels, and direct quotes—starting with the MEMORY lane covering the CLAUDE.md hierarchy, load order, import mechanisms, lazy loading in subdirectories, the auto memory directory feature and when it was introduced, the # shortcut and /memory command, compaction thresholds and hooks, and how context window composition works.

For the TOOLS lane, I'm mapping out skill definitions versus MCP versus plugins versus slash commands across the documentation, tracking where skills live in the filesystem, what SKILL.md frontmatter contains, how progressive disclosure works, slash command syntax with arguments and allowed-tools, plugin marketplace installation and components, and the find-skills CLI tool.

I'm also uncertain about some details like whether there's a Vercel "skills" package or if "skills.sh" is the community registry that find-skills queries, so that's worth verifying through the agent. Then there's tool lazy loading with MCP tool search for deferred tools, the full event list for hooks with matchers and JSON output fields, settings precedence, and the permission system with allow/ask/deny rules, permission modes, and sandboxing capabilities.

For subagents, I need to check the docs on agent configuration files, frontmatter fields like name and description, auto-delegation based on the description, how they're invoked via the Agent/Task tool, whether nested subagents are allowed, context isolation, background execution, concurrency limits, and the SendMessage continuation feature.

For the cloud lane, I'm looking at Claude Code on the web as it moves from research preview to general availability, the isolated VM sandbox with network restrictions, GitHub app integration, how to launch from web or mobile, teleporting sessions between web and CLI, running multiple cloud sessions in parallel, whether MCP is supported in cloud, managing secrets and environment variables, using GitHub Actions as a headless cloud agent, Agent SDK deployment options, pricing and limits, and how cloud differs from local execution.

I'll have each agent cite evidence from code.claude.com/docs in structured markdown format with confidence levels, flagging anything unconfirmed as unknown. Before finalizing the prompts, I should check if the master bank already defines the YAML schema for part 7 and the matrix structure for part 6, likely in the distill-session skills folder.

For context, I'm Claude Fable 5 running in Claude Code Desktop on Windows 11, so I'll note that in the provenance. The user's request for "multitask four lanes" is explicit enough that I can handle it with four parallel Agent calls—no need for Workflow orchestration.

Now I'm mapping out the tool sequence: first I'll read the netie-distill rules file and glob through the skills directories to find relevant distillation content, then search for files containing "PART 6", "PART 7", or "Master Bank" references. Once I've located those files, I'll check the parking lot, spawn the four lane agents with carefully crafted self-contained prompts, gather some CLI observations, and write everything out.

For the lane agents, each one needs to research the official Claude Code documentation and Anthropic sources, then return a markdown list of claims with evidence links, confidence levels, and notes—keeping it tight between 25-60 claims per lane. I should also run `claude --help` to capture what the CLI actually shows on this machine.

I'm deciding which help commands to run: `claude --help` is essential for the richest output, and I'll pair it with `claude mcp --help` and `claude plugin --help` to cover the main subcommands, running them together in one call to keep things efficient.

I also want to explore the project transcript files in the `.claude/projects` directory using PowerShell's `Get-ChildItem` to see what step logs exist, and check for `~/.claude/settings.json` to understand the permission and hook configuration format — just a light read of the JSON files there to document what's observable.

Now I'm laying out what I've already observed from my own context about the execution flow and turn lifecycle to seed the documentation before fusing in the agent results. Subagents can isolate their work using separate git worktrees or remote cloud environments, and they inherit the session's credentials by default while accessing MCP servers through ToolSearch, though interactive authentication may not work in headless contexts.

The system logs full transcripts as JSONL files organized by project and session, with workflows maintaining separate journal and agent-specific logs, and the CLI can emit structured step events in JSON format when run headless. The final output must contain everything the user needs in the summary message.

Plan mode uses question prompts and approval gates to guide research in read-only state, while skills are packaged instruction sets that load on invoke—either running in a subagent or returning results directly. Skills appear with brief descriptions in the system prompt for progressive disclosure, and plugin skills use namespace prefixes like `plugin:skill` to organize them by source.

MCP tools follow a similar namespacing pattern and can inject instructions into the system prompt, though auth-gated servers require OAuth setup before they're usable. Tool schemas use lazy loading—they're only fetched when needed, which means calling an unfetched tool triggers a validation error. Hooks exist to intercept tool calls and treat their output as user feedback.

Permission tiers let users choose allow/ask/deny modes for tool calls, and custom slash commands surface as skills in this environment while built-in CLI commands don't. The Skills CLI and find-skills command aren't directly observable here but are documented elsewhere. I'm also seeing how per-turn context gets assembled from the system prompt and injected content.

The memory system stores one fact per file in a project-specific directory with frontmatter and wiki-style links, with a MEMORY.md index that always loads; there's a consolidate-memory skill for managing this. When conversations grow long, the system summarizes context and carries forward the summary plus remaining context to the next window. There's no automatic vector RAG over the repo itself. remoteTrigger tool exists but its name is deferred. There's a /code-review ultra feature for multi-agent cloud review that's user-triggered and billed, requiring a git repo. The rest covers documentation around claude.ai code sandbox, GitHub integration, environment configuration, secrets management, teleport, Agent SDK deployment, and the claude-code-action. Subagents are defined through frontmatter in .claude/agents/*.md files specifying model, effort, and tools, invoked via the Agent tool with a subagent_type parameter, returning a final report that gets relayed rather than shown directly to the user, with continuation possible through SendMessage.

I'm working through how context persists across calls, whether fresh calls reset to background defaults, how worktree isolation works, model override parameters, agentType in workflows, and forced structured output via schema. For nesting, Explore excludes Agent from its tools, and the docs indicate subagents can't spawn subagents—I should verify that. I need to find the master bank file to complete the decision matrix and YAML sections, then craft focused prompts for the four agents asking for claim-URL-confidence lines with 20–45 claims each, directing them to code.claude.com/docs and requiring exact doc paths.

The claude-code-guide agents have Read/Glob/Grep/WebFetch/WebSearch capabilities and likely access a bundled docs cache, which is helpful for Claude Code questions. Since no existing claude-code-guide agent is running, spawning four distinct ones for separate scopes is fine. I'll let them run in the background while I handle repo reads, then synthesize once they notify completion.

If I can't locate the master bank, I'll construct PART 6 and PART 7 matrices covering key dimensions like planning, parallelism, tool ecosystem, memory, cloud, and governance hooks—filling only the Code column as instructed and noting the bank wasn't found. The master bank is likely in .cursor/skills/distill-session/, so I'm starting a parallel batch of reads: the netie-distill.mdc rules file, glob patterns for the distill-session skills directory, and a grep search for PART 6, PART 7, and MAST references.

I'm also checking the .claude/projects directory for any step logs, then I'll follow up based on what I find. For the final summary, I'll keep it concise—one finding per lane with a one-line path suggestion at the end, exactly as requested. I'm aiming for a curated capture file around 450–600 lines with every claim properly tagged as either observed from this session or from training knowledge. I'll spawn the agents once I understand the bank format so I can brief them on any PART specifics they need. For tracking every step, I can reference the session transcript in JSONL format, headless stream output, or the workflow journal with OTEL telemetry for audit purposes. Now I'll write the first response with a brief plan statement.
'm seeing the step log structure on disk with session JSONLs, subagent files, and workflow journals—good evidence of what's been tracked. Now I need to review the master documentation files including the interrogation templates, distillation guides, and the original prompt they provided to understand the full context.
I've located the master bank at `skill_distill/prompts/MASTER_INTERROGATION.md` and confirmed the step-log claim is already recorded in the session files. Now I'll read through the bank formats and distill conventions.

Read 4 files
Now I'm spinning up four lane agents to run in parallel while I gather local evidence—checking the Claude CLI help output, the MCP help, reviewing some key documentation and session files to understand the event structure and metadata format.

For the lane prompts, I'm setting up specialized agents focused on Claude Code documentation, each with a tight scope and clear output contract. They'll deliver raw markdown claim lines with URL citations, and I'm making sure they know today's date and that the docs are at code.claude.com/docs. I'm asking for 20–45 claims per lane and instructing them to explicitly mark UNKNOWN where the documentation doesn't confirm something.

Lane A is focused on memory—covering CLAUDE.md types and load order across enterprise/user/project/local/subdir scopes, @imports, rule precedence, the auto memory directory structure, documentation of the /memory command and /init, compaction triggers and thresholds, the /compact command, microcompaction, PreCompact hooks, and what gets preserved through compaction.

Lane B covers tools, skills, and MCP—defining what a skill is with its SKILL.md frontmatter fields, where skills live across different directories, progressive disclosure of metadata versus body content, slash commands with their frontmatter configuration, plugins from the marketplace with their component structure, and MCP setup including the claude mcp add command, scope levels, transports, OAuth, and how tool naming and deferred loading work.

Now I'm looking at hooks—the full event lifecycle from PreToolUse through SessionEnd, how matchers and JSON output fields configure behavior (continue, stopReason, suppressOutput, permission decisions, updatedInput, additionalContext), the exit-code protocol, and permissions management through settings.json with allow/ask/deny arrays and Bash rule syntax.

For agents and subagents, I'm considering the definition structure in .claude/agents/*.md files with frontmatter for name, description, tools, model, and potentially permission-mode or isolation settings, how the /agents command and auto-delegation work based on descriptions, the return shape (final message with separate context), parallelism constraints, and nesting capabilities.

I'm also thinking through tool allowlist inheritance where omitting tools means inheriting everything including MCP, model inheritance versus explicit selection, agent teams if documented, how they differ from skills, and the SDK agents parameter in query options, plus cost and latency implications.

For the cloud side, I'm mapping out Claude Code on the web with its sandboxed VM per task, network access rules, GitHub integration, branch and PR workflows, teleporting between web and CLI, parallel sessions, and mobile support, along with environment configuration like secrets and setup scripts, whether MCP works in cloud sessions, and the OS-level sandboxing mechanisms.

I'm also covering GitHub Actions integration with Claude mentions and the claude-code-action, secrets management through repo secrets, the Agent SDK for headless deployments in containers, scheduled cloud agents or routines from desktop if available, billing implications for web usage, and how cloud differs from local in terms of permissions, sandbox defaults, filesystem access, and credential isolation.

I'll structure all findings with exact documentation URLs, using WebFetch for code.claude.com/docs pages and WebSearch to locate them, aiming for 25–45 claims per section formatted with evidence links and confidence levels, then compile unknowns at the end.

Running all four research tasks in parallel while I also check local observations—pulling help output from the Claude CLI, checking for related commands like `claude mcp --help` and `claude plugin --help`, reviewing the PARKING_LOT.md file for reference P19, and reading the first few lines of the session transcript to see what's already been captured.

For the local evidence batch, I'm using PowerShell to truncate large JSON lines to the first 600 characters so I can see the event structure (type, uuid, parent, timestamp fields), doing the same for subagent meta.json and journal.jsonl files, then listing the top-level directories under ~/.claude and D:\Cortex\.claude to observe where settings, skills, agents, and plugins are stored.

On PARKING_LOT.md, I should grep for P19 with surrounding context rather than reading the whole file, and I can skip skill_distill/README.md and ASK_CLAUDE_CODE.md since I already have that information. One thing I'm wondering about: the netie-distill rule mentions running `python scripts/distill_ingest.py` after capture, so I need to figure out whether that should happen as part of this workflow. I'll run the capture with a scoped file argument, then review the distill_ingest.py script to understand what it does. After that, I need to write the agent prompts—each one self-contained with explicit output formatting, including today's date, the primary documentation URLs, and instructions to use WebFetch/WebSearch for fact-checking, flagging any unconfirmable claims under UNKNOWNS with suggested experiments. The final output should be only the markdown claim list.
I've gathered the required formats and am now launching four parallel research agents while collecting local evidence from CLI help, configuration files, and existing logs.
running
Memory lane docs research
cladue code fable 5 internal mind is as above
Memory lane docs research

Tools/skills/MCP lane research

Subagents lane docs research

Cloud agents lane research

Showing Claude CLI version and help >

Inspecting .claude dirs and transcript/step-log file formats >

Searching P19 >

ooking at the `claude --help` output, I'm seeing a bunch of new flags and features in v2.1.212 — there's agent support with JSON configuration and background agents, a new `claude agents` command, worktree support, and permission modes with some interesting newer options like `plan` and `dontAsk`. There's also effort levels, a `--bare` flag that skips auto-memory (confirming that's a real feature), structured output options like stream-json and json-schema, budget controls with `--max-budget-usd`, and MCP support.

Beyond that, I'm seeing even more flags like strict MCP config options, session persistence, `--fork-session` and `--from-pr` for branching workflows, `--remote-control`, and `--disable-slash-commands` which confirms that slash commands are actually skills. There's plugin support with directory and URL options, and enterprise gateway features for auth and telemetry.

The filesystem layout under `~/.claude` shows projects, sessions, tasks, plugins, shell snapshots, and configuration files. Subagent metadata includes a `spawnDepth` field that models nesting levels, and session journals use content-addressed caching with v2 hashes for resuming workflows. I'm also noticing that this directly addresses one of the P19 debts about capturing the Claude Code upgrade information, though there's a reference to an existing capture file that wasn't in my earlier search results.

Now I want to check the actual settings files in use, peek further into the session journal to find assistant and tool_use events, and examine the ingest script to understand how my captures get parsed and formatted so I can match the expected structure.
I've found strong evidence of agent infrastructure in CLI v2.1.212 including agents management, background execution, permission modes, auto-memory controls, and spawn depth tracking. Now I'll check the settings and ingest scripts to ensure pipeline compatibility.

Read distill_ingest.py, ran a command

'm seeing the structure of how Claude's transcript logging works—the JSONL events capture queue operations and user messages with their parent UUID chains, sidechain flags, and prompt IDs, while attachment deltas track lazy-loaded content like deferred tools, agent listings, and MCP instructions. The user settings also reveal plugin configuration and custom marketplace sources including Anthropic's own plugin registry.

For the ingest script, I need to match its exact fact-table format with four columns (fact, observed/docs/inferred, confidence level, promote flag) and include any promote directives as YAML lines that get counted toward parking debts. Rather than drafting the capture skeleton now, I should wait for the agent notifications to complete—the harness will reinvoke me once they finish—then write the full file with both observed evidence and inferred results together. In the meantime, I could review the prior learned documents to identify what claims already exist so I can mark any upgrades, but it's better to just wait for the agents to finish their work.
I'm formatting the table capture for the ingest script and pulling the prior learned files for the PART 6 matrix baseline along with two additional CLI help resources.

Read claude_code_vs_cursor.md, ran a command
I'm also seeing the `claude mcp` subcommands for managing transports and OAuth flows, plus the ability to run Claude Code itself as an MCP server with project approval workflows. There's a `claude plugin` system that shows projected token costs, includes an eval harness with graders, and auto-loads skills from a local directory into the marketplace. The `claude agents` command appears to handle background agent fleets with dispatch definitions.

I've filled in the Code column of that matrix I found earlier. Now I'm waiting for the four lane agents to complete their work — there's no point duplicating their efforts. The agents will notify me when they're done, which will resume my turn, so I can end this segment and let them run while I wait for their results.

I'm debating whether to draft a skeleton of the capture file now in the scratchpad to save time later, with placeholders for the documentation confirmations the lanes will provide. But writing it twice risks inconsistency, and the guidance suggests waiting for all four notifications before writing the final version once with complete information.
All local observed evidence is in: CLI surface (claude --help, mcp, plugin, agents subcommands), settings files with live permission rules, transcript JSONL event shapes (including the deferred_tools_delta / agent_listing_delta lazy-load events and subagent spawnDepth), workflow journal cache keys, and the ingest contract. Notable finds so far:

Lazy tool loading is confirmed and logged — deferred tool schemas load via ToolSearch, and the session step log records deferred_tools_delta events when schemas are injected.
claude plugin details prints a "projected token cost" per plugin — Claude Code literally budgets context cost of tool catalogs, which is exactly the Netie discovery concern.
claude agents is a background-agent fleet manager with per-dispatch model/effort/permission-mode defaults; claude mcp serve makes Claude Code itself an MCP server.
Subagent metadata records spawnDepth, and project-scope .mcp.json servers sit in a "Pending approval" state until approved — a governance pattern worth copying.
The four lane agents (memory | tools | agents | cloud) are still researching official docs in the background. When they report, I'll synthesize everything into the ingest-compatible capture under skill_distill/captures/, run the ingest, and post the summary with the path line.

I'm seeing documentation about Claude's memory features come through with details on context windows, token budgets, compaction semantics, and the PostCompact hook, along with prompt caching TTLs and path-scoped rules. Three other lanes are still pending, so I'll hold off on any final output and wait for the rest to arrive.
Memory lane is in — highlights: exact per-turn prompt assembly budget is documented (system prompt → MEMORY.md excerpt → env → deferred MCP names → skill descriptions → CLAUDE.md layers), compaction has documented survive/drop semantics (root CLAUDE.md re-injects, path-scoped rules are lost until re-triggered), and there's a PostCompact hook plus 1-hour cache TTL on subscription. Three lanes (tools, agents, cloud) still researching — I'll synthesize the capture when they report.

oking at the agents feature rollout—there's a new rich set of frontmatter fields available like skills, memory, and MCP servers, with support for nesting subagents up to a configurable depth. The background field defaults to v2.1.198 and newer, agents were renamed from tasks back in v2.1.63, and there's experimental agent teams functionality plus subagent output scanning starting in v2.1.210.

I'm noticing a discrepancy between what the agent claims and what I'm actually seeing in this session. The docs say there are no built-in specialized agents beyond general-purpose, but I'm observing Explore and Plan as actual agent types in my list here—so either the docs are outdated for this build or there's a difference between versions. I'm flagging both observations and marking this as a medium-confidence discrepancy since what I'm seeing directly contradicts the paraphrase.

The harness is actively sanitizing subagent outputs too—I can see the evidence in the control tags showing when instruction-shaped patterns get neutralized. Still waiting on the tools lane to come through before I can write up the full capture.
Agents lane is in — notable: subagent frontmatter is richer than commonly known (skills, memory, mcpServers, maxTurns, background, effort, permissionMode, disallowedTools), nesting is env-gated via CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH (default off, max 5 — matching the spawnDepth field I observed on disk), and v2.1.210+ sanitizes instruction-shaped subagent output — which I just witnessed live: this very lane's report came back with a harness neutralization banner. One lane (tools) remaining.

Now I'm organizing the capture file structure with the YAML frontmatter containing metadata like id, source, and date, followed by the raw answer section with all four lanes, then the extracted facts table that the ingest regex will parse, the action YAML blocks, and finally the Netie implications section.

I need to be careful with the Netie references — I'll stick to the actual APIs and paths I've seen in the codebase like the DAG runner, memory routes, discovery functions, and the agent task system rather than inventing anything new. The master bank already names these concepts, so I can reference them directly in the netie_equivalent fields using the actual file paths from the repo.

For the output, I need to make sure I'm covering all the requirements: full answers to the focus questions, the Code column in the matrix, deciding what to build now versus park, five experiments, and the path line at the end. I'm organizing this around the user's goal flowing through plan mode, task graph generation, execution, and a final summary.

Once the plan gets approval, I move into execution using the task tracking tools. I can parallelize work through concurrent tool calls, spawn subagents for background processing, use the Workflow tool for deterministic fan-out up to 16 concurrent operations, and leverage agent teams if available. For sharing and isolation, agents use the same filesystem by default with optional worktree isolation, and the agent view command shows worktree status.

Cloud tasks run on separate VMs with their own branches. MCP servers are inherited by subagents, and workflow agents can search for tools dynamically. When failures occur, parallel operations resolve to null and pipeline stages drop on errors, while agents can be skipped mid-run. Session logs are stored as JSONL files locally with metadata tracking spawn depth and tool use IDs.

For understanding execution flow, I'm considering transcripts, stream JSON output, and hook events to capture what's happening at each step. I need to focus on what's actually verified rather than inferring capabilities. Now I'm defining the distinction between skills, MCP servers, plugins, and slash commands, with skills being defined through SKILL.md files that include metadata like descriptions and allowed tools.

Skills follow a progressive disclosure pattern where metadata is always available but the full body loads on invocation, and they can be auto-invoked based on description matching unless disabled. I'm noticing the tools lane didn't confirm a find-skills command or npx skills tool—instead they pointed to plugin marketplaces and discovery mechanisms like the Claude plugin marketplace and community repositories.

Looking at what I've observed: Cortex has its own FIND_SKILLS.md documentation, and there's a find-skills skill in the anthropics/skills repo from my training data that installs via marketplace, though the tools lane couldn't verify this. The honest assessment is that the official installation path today uses plugin marketplaces (like `claude plugin marketplace add`) and `claude plugin init` for scaffolding skills into ~/.claude/skills, with find-skills as an inferred helper skill in the anthropics repo—medium confidence since I can't confirm it directly from the lane's documentation.

Skills in ~/.claude/skills auto-load with the naming convention <name>@skills-dir. For lazy loading, there's an ENABLE_TOOL_SEARCH environment variable (defaults to on, with options for true/auto/auto:N/false) that defers tool loading and saves context budget—MCP tool names are deferred around 120 tokens, and there's a MAX_MCP_OUTPUT_TOKENS limit of 25k. The system also supports a rich set of hooks for lifecycle events like SessionStart/End, UserPromptSubmit, PreToolUse/PostToolUse, and various others across 20+ event types.

Hooks can intercept tool calls and return different command types (command/http/mcp_tool/prompt/agent) with exit codes and JSON responses that control flow—things like continue, stopReason, decision, suppressOutput, systemMessage, and permissionDecision (allow/deny/ask/defer). Permissions follow a 6-step evaluation order through hooks, deny rules, ask prompts, mode settings, allow rules, and finally canUseTool checks, with the rule syntax visible in settings.local.json and modes available in the help output.

Sandboxing operates at the OS level using Seatbelt on macOS, bubblewrap on Linux, and WSL2 on Windows, restricting filesystem access to read-all and write to the current directory plus temp folders, controlling network access through domain allowlists, and masking credentials with optional TLS termination. MCP scopes work across local/project/user levels with project approval states tracked in .mcp.json, and transports are configured through standard mechanisms.

For plugins, the manifest lives in .claude-plugin/plugin.json with components like skills, agents, hooks, and MCP integrations namespaced with colons, versions pinned by SHA, and token costs visible in the details view. Memory assembly per turn works within a token budget, with the system prompt around 4200 tokens, MEMORY.md excerpts around 680 tokens from the first 200 lines, environment variables around 280 tokens, and deferred MCP names handled separately.

CLAUDE.md files exist at managed, user, project, and local scopes with lazy-loaded subtrees and @imports up to 4 hops deep, plus path-scoped rules in .claude/rules/*.md that get compacted away and re-triggered. The /init command supports CLAUDE_CODE_NEW_INIT, /memory accesses the memory system, and the # shortcut for memory lane is documented historically though not explicitly confirmed in observations.

Auto-memory defaults to enabled with a /memory toggle and CLAUDE_CODE_DISABLE_AUTO_MEMORY=1 environment variable to disable it, while --bare skips it entirely. The MEMORY.md file loads around 200 lines and 25KB with topic files fetched on demand. Compaction runs automatically near the limit or via /compact command, with PreCompact and PostCompact hooks that preserve the root CLAUDE.md and unscoped rules through the process.

Sessions store in ~/.claude/projects/<slug>/<id>.jsonl with an unstable format containing queue operations, parent UUID chains, and attachment deltas, though /export provides a stable format. The system cleans up sessions after 30 days by default, and supports --continue, --resume, --fork-session, and --from-pr flags to manage session continuity. The /branch command inherits session permission grants. Retrieval uses agentic search with grep and glob patterns rather than a built-in embedding index.

The codebase recommends exposing RAG through MCP tools, and there's an mcp__ccd_session_mgmt__search_session_transcripts tool available on desktop for on-demand session transcript searching. Prompt caching uses multiple invalidation layers triggered by model switches, effort changes, MCP connections, plugin toggles, /compact commands, or upgrades, with a 1-hour TTL for subscriptions versus 5 minutes for API calls. Editing CLAUDE.md mid-session doesn't take effect until reload, and there's an --exclude-dynamic-system-prompt-sections flag for cross-user cache optimization.

For the cloud environment, code review tasks run in isolated Anthropic VMs with 4vCPU/16GB/30GB storage, fresh clones, claude/* branches, and PR workflows supporting parallel execution and mobile QR access on premium seats. Permissions use auto-accept with sandboxing replacing prompts, network access is restricted to a trusted allowlist with proxy enforcement, and GitHub credentials are injected via proxy rather than exposed as raw tokens in environment variables. The bash setup script caches for about 7 days using .env-style variables without local config, and CLAUDE.md must be committed to the repository. MCP cloud connectors operate through HTTP/SSE via Anthropic servers.

MCP configuration uses .mcp.json for HTTP connections, with stdio not available. Teleport supports --cloud and --teleport experimental flags along with --remote-control for long-running tasks on your own hardware. Routines trigger via cron, API, or GitHub events, each execution runs in an isolated session with POST /fire requests using bearer tokens and untrusted-payload wrapping, subject to daily caps and no interactive prompts. GitHub Actions integration uses the claude-code-action@v1 with @claude mentions and ANTHROPIC_API_KEY secrets, running on GitHub runners rather than Anthropic VMs. The Agent SDK deploys as a self-hosted process or container with headless streaming via JSON output.

Managed Agents are Anthropic-hosted with REST and SSE endpoints running in a gVisor sandbox. Billing shares subscription usage, though ZDR isn't compatible with the web interface. Subagents are defined via configuration files with full frontmatter metadata, invoked automatically through descriptions or explicitly via the Agent tool, returning only the final message to the user. They inherit the system prompt and available tools but maintain separate context without access to conversation history or auto-memory. Background execution defaults to enabled in version 2.1.198+, with SendMessage supporting continuation through harness features. Nesting is environment-gated with a maximum depth of 5 levels, controlled by spawnDepth settings.

Tool allowlists inherit by default with disallowed tools restricting access including MCP patterns. Model aliases and effort settings apply per agent, while built-in tools are general-purpose. The Agent tool has no documented hard cap for parallel execution, though workflows support up to 16 concurrent operations with a total limit of 1000. Subagent output gets sanitized starting in version 2.1.210+. Skills, MCP servers, and memory fields are configurable, with agent teams currently in experimental status.

Now I'm filling in the decision matrix for the Code column across those seven capability rows—one-shot multi-step coding, memory across sessions, MCP connectors, skills packaging, governance and audit, cloud scale, and UI for non-developers. I'll pull the existing learned values from the comparison document and update just the Code column with high-confidence details. Then I'm mapping build-now candidates versus parking items to Netie concepts, distinguishing between what should be promoted as rules, skills, or subagents versus what stays in the backlog. al with content-addressed keys for resumable workflow runs, compaction semantics that reinject durable memory and retrigger path-scoped context, and token-costed capability manifests that project costs into pack metadata. I'm also noting several items to park for now: agent teams pending stability, managed agents API parity, sandbox credential masking with a TLS proxy, and session migration.

Continuing with the remaining parked items and moving into the experiments: I need to resolve the classifier-approved permissions and the exact install path for find-skills, then run five Playwright and CLI experiments to validate step logging with hook events, lazy-loading behavior with and without tool search enabled, and token context deltas.

For the parallel isolation test, I'm checking whether two background sessions writing to the same file trigger conflicts under the default shared filesystem, then verifying that worktree isolation prevents this and auto-cleans up. The nesting experiment sets a spawn depth limit and confirms that subagents respect the spawnDepth metadata and that nesting is off by default. Finally, I'm driving the claude.ai/code interface to launch a task on a test repo and observe branch creation and sandbox environment behavior. Narrowing down the action list to the major ones the bank cares about — search memory, call MCP, spawn subagent, compact context, switch model, deploy cloud agent, install skill — plus a few supporting actions like tool search, gate hooks, routine firing, and planning. That gives me around 12 YAML blocks total, each with trigger conditions, what it uses, inputs, and outputs.

Now I'm mapping these back to the actual codebase paths I can see in git status to ground each action in real implementation files across memory routes, semantic caching, MCP integration, execution engines, and the API layer.

I need to extract the key facts into a structured table with confidence levels and promotion flags, then write this out to a markdown file with the proper frontmatter including the operator info and prompt reference. I need to decide whether to mark the first debt item as complete since the capture now exists, and update the DISTILL.md checklist for the Claude Code section. The ingest script handles P19 additions automatically, so I'll make a minimal edit to check off that one debt line with the capture path, then update the relevant checklist boxes in DISTILL.md since the capture fulfills those topic requirements — being careful not to overwrite other changes since PARKING_LOT.md is being modified elsewhere. Now I'm ticking the eight boxes under Claude Code agentic runtime in DISTILL.md — plan mode, multitask, subagent, MCP, hooks/permissions, compaction, and cloud all check out, but I'm leaving Skills CLI unchecked since find-skills is still unresolved. Though I need to be careful: if I run ingest with --capture pointing only to mine, _list_captures will return just that one, and _update_index will rewrite INDEX.md with only that capture's data.

That's a problem — it would wipe out the 2026-07-24 claude-app entry that's already there. The safer move is to run the ingest script without the --capture flag so it processes all captures and rebuilds the full index correctly, then P19 debts appends only new unique lines as expected.

I should also verify Python is available on the PATH and end with a one-line path suggestion as requested. The tools lane URLs use .md suffixes while memory lane doesn't — both are fine as given. For the "# shortcut" on memory lane, I'll mark it as inferred with medium confidence since it wasn't explicitly confirmed. On find-skills, I'm marking UNKNOWN/inferred entries with medium confidence and noting what I've observed about how Claude scaffolds skills into ~/.claude/skills and the known marketplaces.

Auto-suggesting skills works through description matching with an option to disable model invocation. When Claude auto-suggests, it's based on task-description alignment. Permission tiers follow a deny/allow/ask pattern with a six-step ordering, and I've observed the CLI modes include acceptEdits, auto, bypassPermissions, manual, dontAsk, and plan. Context compaction triggers automatically during long coding sessions when approaching the window limit, can be manually triggered with /compact, and uses PreCompact hooks — the harness describes this as summarizing some or all current context.

Cloud deployment spans web UI, desktop, and mobile dispatch with a --cloud flag, supporting routines, GitHub Actions, and SDK-managed agents. Scaling uses parallel VMs per task with daily routine caps and resource limits, while the sandbox runs on gVisor with a proxy layer. Secrets are stored in environment config with GitHub credentials proxy-injected, and the sandbox masks credentials locally while cloud environment variables are stored per environment.

Subagents return their final message as the agent tool result, and when called in workflows, the agent function returns either text or a schema-validated object. Nesting limits are disabled by default but configurable via environment variable, with a documented maximum of 5 levels — my build version falls within the documented range.

Tool allowlists are controlled through the tools field, disallowedTools, mcp__* patterns, and CLI flags like --tools, --allowedTools, and --disallowedTools. For parallel tasks sharing resources: same-session subagents share the working directory, filesystem, and git branch unless isolation:worktree is set, they inherit MCP and skills from session defaults, and secrets are shared through the same process environment. Background agent view sessions operate separately with worktree file isolation managed by the supervisor.

Cloud-hosted task agents get separate VMs and branches, connect through Anthropic-hosted connectors, access secrets via environment configuration, and interact with GitHub through a proxy. Workflow agents share the filesystem unless worktree isolation is enabled, and they share budget and concurrency limits. When failures occur, parallel workflows return null, pipeline stages drop items on errors, and agents become null after exhausting retries or when users skip them—siblings continue without cascading cancellation. The agent tool sends notifications on completion and handles retries internally.

Now I need to update the decision matrix's Code column with the full table, keeping other columns unchanged from the learned file. The learned file indicates I should upgrade the Code column to high confidence after this live paste, so I'll update that file and add my capture to its distill citations as part of the promotion workflow. I'm creating the capture file, running the ingest script, then editing the learned file to reflect the updated Code column assessment.

Writing the capture document... The CLI supports multiple execution modes including interactive, headless with text or JSON output, background agents, plan mode, cloud-based sessions, and remote control options. The configuration lives in several locations: user-level settings in ~/.claude/ with subdirectories for projects, plugins, sessions, and credentials, plus project-specific overrides in .claude/ and .mcp.json files. The execution flow starts with goal intake through plan mode, which uses tools to read requirements and ask clarifying questions before transitioning to task graph generation.

Once approved, the system creates tasks through dedicated APIs and executes them with support for parallel processing across multiple mechanisms. The final summary must contain everything the user needs in the text message itself. I'm also tracking the sharing model across different mechanisms like filesystem, git, MCP, and secrets, plus failure handling semantics and step logging through session JSONL files.

Now I'm looking at the event shapes flowing through the system — queue operations with user/assistant messages, parent UUIDs, sidechain flags, and deltas for deferred tools, agent listings, and MCP instructions. Subagents maintain their own JSONL logs with metadata about agent type and spawn depth, while workflows use content-addressed journal entries. The documentation distinguishes between internal transcripts and stable exports with a 30-day cleanup period, and headless mode supports streaming JSON output with hook events and subagent forwarding.

For tools and skills, there's a definition split across skill types, slash commands, plugins, and MCP servers, each with their own disk home and loading mechanism. Skills have frontmatter fields and progressive disclosure through agentskills.io, with auto-invocation controlled by description matching and model-invocation toggles. The CLI provides plugin installation and initialization, auto-loading skills from a designated directory, and tools to check token costs and evaluate harness performance.

The find-skills command itself uses lazy loading with deferred tool lifecycle — tools are listed first, then selected through ToolSearch, with schemas loaded only when needed to avoid InputValidationError on direct calls. This defers around 120 tokens of MCP names versus full schemas, with a 25k token limit for MCP output before overflowing to file. There are five handler types for hooks (command, HTTP, MCP tool, prompt, and agent) that respond to various lifecycle events like session start/end, user prompts, tool use stages, and subagent operations.

Permission handling uses a tiered system with Restrict/Allow/Request modes that evaluate in a 6-step order, supporting different CLI build modes from manual to fully automatic with optional bypass. The sandbox applies OS-level mechanisms with default restrictions on filesystem and network access, credential masking, and TLS termination, though there's a parameter to disable sandboxing if needed.

MCP integrates scopes and approval workflows through .mcp.json configuration, handles OAuth login/logout flows, and supports various transports with environment variable expansion in headers. Plugins use manifests with LSP and monitoring components, versioned by SHA and namespaced, with marketplaces discoverable through user settings. For scaling across many tools, deferral defaults prevent token bloat while tool ranking relies on model-driven searches, though interactive OAuth becomes unavailable in headless environments.

Now looking at how toolsets can pivot mid-session through schema deltas, with MCP reconnections invalidating caches unless deferred, and permissions reloading through settings. Memory assembly works per-turn with a documented token budget, using a CLAUDE.md hierarchy with lazy subtree loading and path-scoped rules. The system skips automatic chat-RAG but exposes session transcript search as an on-demand MCP tool on desktop, with session management commands like resume, continue, fork, and from-pr. Compaction triggers determine what context survives or gets dropped based on configured hooks.

For retrieval, I'm thinking about agentic grep and glob patterns, with large codebases relying on bring-your-own RAG via MCP. Prompt caching uses layered invalidators with TTL windows, where mid-session CLAUDE.md edits don't trigger cache invalidation unless dynamic system prompt sections are explicitly excluded. Cloud agents span multiple deployment paths—web UI, desktop dispatch, mobile, cloud flags, local background tasks, routines, GitHub Actions, SDK, and managed agents—each with its own sandbox model using VMs per task, gVisor proxying, resource limits of 4vCPU/16GB/30GB, and writes isolated to the working directory and temp storage, plus a trusted network allowlist and GitHub credential injection through a proxy.

For environment and secrets, I'm looking at setup scripts that cache credentials for about seven days, environment-specific .env variables without local config, and the requirement that CLAUDE.md be committed to the repo. MCP in the cloud uses HTTP/SSE connectors through Anthropic since stdio isn't available. There's an experimental teleport flag for cloud deployments and Remote Control for long-running tasks on own hardware. Routines fire on cron, API, or GitHub triggers with isolated sessions per invocation, untrusted payload wrapping, daily caps, and permissionless sandbox execution. GitHub Actions runners differ from the web VM setup, and I'm considering how parallel branches and billing scale together.

Now looking at subagents—they're defined with a full frontmatter field list and invoked either automatically by description or explicitly by name using the Agent tool. They run in the background by default, returning only the final message which isn't shown to the user but relayed through workflows that enforce StructuredOutput validation. Each subagent gets a fresh context window inheriting the system prompt, CLAUDE.md, and available tools per session, but not conversation history or auto-memory, with continuation handled through SendMessage.

Nesting is controlled by spawnDepth with an environment variable cap of 5, and tools are gated through allowlists and disallowedTools patterns configurable via CLI. Different agents have different model assignments and effort levels, and parallelism is uncapped per session for the Agent tool but workflows enforce a live limit tied to core count with lifetime and per-call caps. Output goes through sanitization.

Looking at the newer builds from 2.1.210 onward, there's worktree isolation as an opt-in feature and agent teams available under an experimental flag. I'm mapping Cursor Tasks at the concept level rather than field-by-field, referencing existing Cursor capture patterns. There's a matrix to update with code column changes, a promotion map with distill anchors and tags, five validation experiments pulling from existing Cortex benchmarks and demos, an extracted facts table, and a YAML action block with twelve sections covering the Netie implications.

I need to reorganize the final output structure — the template puts Action YAML before implications and citations, but the requirement is to end with those YAML blocks, so I'll move that section to the very end after everything else. The ingest process only needs the table and promotion regex, which can go anywhere, but the citations pull from the distill captures and prompt files along with documentation URLs. Generating markdown table...
All four lanes are in. Synthesizing the ingest-compatible capture now — writing the full evidence-tagged document, then running the distill pipeline (ingest → learned matrix update → P19 debt tick).

Capture synthesis & ingest

Below is Claude Cowork in a project 
The full distill is delivered above and also saved to this Project as claude/netie-distill-master.md so future sessions can build on it.

How I sourced it, so you can weigh the answers: Claude Code facts (hooks — all ~29 event names, payload shape, exit-code-2/JSON deny channels; settings precedence; subagent frontmatter incl. depth-limit 5; the six permission modes; compaction behavior) were verified today against the official docs via a research subagent. Orchestration semantics (deferred tool loading, Workflow pipeline/parallel/resume, summary-only subagent returns, scheduled tasks) are first-hand from this very runtime, which is Cowork built on the Claude Agent SDK — so Part 0 and much of Parts 2–4 are observation, not paraphrase. Cursor material is from its 2.4 changelog and current third-party guides and is marked medium confidence throughout. Anything server-side that isn't disclosed — memory storage backends, tool-ranking signals, cloud billing/provisioning — is marked UNKNOWN with a matching experiment in Part 8 rather than guessed.

Three findings worth pulling forward: the industry converged on the same primitives (SKILL.md, deferred tool schemas, hook names — Cursor literally adopted PreToolUse/PostToolUse), so Netie should copy those formats verbatim rather than invent; Claude Code's deny → hook → allow → prompt permission pipeline is the strongest governance design available and maps cleanly onto Netie; and the biggest gaps Netie can own are the three silent events no product ledgers today — ambient memory injection, what compaction dropped, and model switches. The closing section has the 30-day list, the parking lot with un-park conditions, and the paste-ready Cursor rule (~1,650 chars) and distill-session SKILL.md.

Sources: Claude Code hooks, settings, subagents, permissions, Claude chat search & memory, Cursor 2.4 changelog, Cursor 3 Agents Window guide, Cursor 3.2 ana
Done — the full distill ran plan → four parallel lanes → synthesis, and the whole pipeline is committed to the repo (uncommitted in git, staged nothing): capture written, ingest run, learned matrix upgraded, P19 debts settled. The save-ready, evidence-tagged answer (every claim carries evidence + confidence, ends with the PART 7 YAML blocks) is at 2026-07-25_claude-code_all-lanes.md. Highlights per focus area:

One-shot plan + multitask. Plan mode is a read-only permission mode with an explicit approval gate (ExitPlanMode offers "yes + auto mode / yes manual / keep planning / no"); execution then runs through a tracked task list plus four parallelism mechanisms: batched parallel tool calls, background-by-default subagents, claude agents background sessions (worktree-isolated, supervisor-kept-alive), and deterministic Workflow scripts (16 concurrent / 1000 lifetime cap). Sharing: same FS + branch by default; worktree or remote isolation is opt-in; secrets are inherited, not scoped per agent (the scoping levers are sandbox credential deny/mask and, in cloud, a credential-injecting proxy). The step log is real and on disk — per-session JSONL under ~/.claude/projects/<slug>/, per-subagent transcripts with spawnDepth metadata, and workflow journals with content-addressed (v2:<sha256>) step keys that power cached resume; headless, it's --output-format stream-json --include-hook-events.

Tools/Skills/MCP. Skill = SKILL.md instruction pack (progressive disclosure), slash command = user-typed skill invocation, plugin = distribution bundle (skills/agents/hooks/MCP/LSP), MCP = external tool server with per-project approval gating and OAuth. Lazy loading is default-on and observable: deferred tools are listed name-only (~120 tokens), ToolSearch injects schemas mid-turn, and each injection is logged in the transcript (deferred_tools_delta) — governed by ENABLE_TOOL_SEARCH incl. an auto:N percent threshold. Hooks span 13+ events with five handler kinds and can allow/deny/ask/defer or rewrite tool input; permissions evaluate hooks → deny → ask → mode → allow → callback, with deny un-bypassable. Restrict/Allow/Request = deny/allow/ask. One miss: the find-skills / npx skills path could not be confirmed from official docs (confidence: low) — parked with an experiment; the confirmed path is plugin marketplaces + claude plugin init (which also prints per-plugin projected token cost and has a graded eval harness).

Memory/RAG. Per-turn assembly is documented with token budgets (system prompt → auto-memory MEMORY.md excerpt (200-line/25KB cap) → env → deferred MCP names → skill descriptions → CLAUDE.md layers). Compaction has precise survive/drop semantics — root CLAUDE.md and auto-memory re-inject from disk, path-scoped rules die until re-triggered — with PreCompact/PostCompact hooks. Retrieval is agentic grep/glob, no embeddings; big-repo RAG is explicitly delegated to your own MCP tools (Netie's RawKnn seam).

Cloud. One gVisor VM per task (4 vCPU/16 GB/30 GB), auto-accepted permissions (sandbox replaces prompts), claude/* branches, connectors-only MCP (no stdio), setup scripts cached ~7 days. Routines are scheduled cloud agents whose POST /fire API wraps external text in an untrusted-payload block — the single best governance pattern found for Netie's webhook ingestion.

Subagents. Frontmatter is richer than commonly documented (tools, disallowedTools, model, effort, permissionMode, skills, memory, mcpServers, maxTurns, background); return contract is final-message-only into a fresh context; nesting is env-gated (CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH, default off, max 5 — corroborated by spawnDepth in on-disk metadata); and since v2.1.210 subagent output is sanitized against instruction-shaped injection — which I observed live twice when my own lane agents' reports came back bannered.

Promote (build-now): deferred-catalog lifecycle w/ ledger-logged injections; permission-pipeline ordering; untrusted-payload wrapping; the subagent contract; content-addressed step journal for DAG resume; compaction re-inject semantics; token-costed pack manifests. Parked: agent teams, hosted-sandbox parity (P17), credential-mask proxy (P17a), teleport, auto-mode classifier, find-skills path. Experiments: E1 stream-json vs on-disk log diff, E2 lazy-load lifecycle + /context deltas, E3 shared-FS vs worktree collision, E4 nesting gate, E5 Playwright on claude.ai/code + routine /fire (homes marked in bench/stress, tests/reliability, dms-ui/e2e).

Pipeline state: ingest processed 4 captures; INDEX.md rebuilt; two P19 debts ticked as settled and three new parking debts auto-appended; claude_code_vs_cursor.md Code column upgraded to live/high-confidence.

skill_distill/captures/2026-07-25_claude-code_all-lanes.md
c:\Users\OoiJianHong\Downloads\netie-distill-master.md 

c:\Users\OoiJianHong\Downloads\2026-07-25_claude-app_capabilities.md , this one is from claude in main ai

For cursor further invoke how it cooridnate multi agnet sand chanc hsoose models via like normal text how the context undersanding and retreival and actions so precise
</user_query>