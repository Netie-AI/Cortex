{
  "install": "curl -LsSf https://astral.sh/uv/install.sh | sh && export PATH=\"$HOME/.local/bin:$PATH\" && uv venv .venv && . .venv/bin/activate && uv pip install -e \".[api,dms]\" && uv pip install playwright && python -m playwright install chromium && (test -d ../openvault/OpenMW && cd ../openvault/OpenMW && uv sync || true)",
  "start": "export PATH=\"$HOME/.local/bin:$PATH\" && export OPENVAULT_ROOT=\"${OPENVAULT_ROOT:-$PWD/../openvault}\" && bash scripts/start_demo_stack.sh",
  "terminals": [
    {
      "name": "demo-stack",
      "command": "export PATH=\"$HOME/.local/bin:$PATH\"; export OPENVAULT_ROOT=\"${OPENVAULT_ROOT:-$PWD/../openvault}\"; bash scripts/start_demo_stack.sh"
    }
  ]
}
